@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<WebApplication1.Models.Profile>
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>SomeView</title>
</head>
<body>
    <div>
        <table>
            <thead>
            <th>ID</th>
            <th>Name</th>
            <th>Surname</th>
            <th>MobilePhone</th>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    <td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Surname)
                    <td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MobilePhone)
                    <td>
                        @Html.ActionLink("Edit", "Edit", "StringFiles", new { id = item.id })
                    </td>
                    <td>

                        @Html.ActionLink("Delete", "Delete", "StringFiles", new { id = item.id })
                    </td>
                </tr>
                }

            </tbody>
        </table>
    </div>
    <a href="/Profile/Create">Add</a>
</body>
</html>